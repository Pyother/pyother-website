import { createTheme } from '@mui/material/styles';

const colorsTheme = createTheme({
    palette: {
        primary: {
            main: '#003366',
        },
        secondary: {
            main: '#ffcc00',
        },
    }
});

export default colorsTheme;