import { createTheme } from '@mui/material/styles';

const colorsTheme = createTheme({
    palette: {
        primary: {
            main: '#003366',
        },
        secondary: {
            main: '#ffcc00',
        },
        dark: {
            main: '#1c2028'
        },
        light: {
            main: '#f0f0f0'
        },
    }
});

export default colorsTheme;